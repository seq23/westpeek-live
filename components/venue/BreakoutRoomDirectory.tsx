import { getBreakoutRooms } from "@/services/networking-ops";
import { SectionCard } from "@/components/shared/SectionCard";
import { StatusBadge } from "@/components/shared/StatusBadge";
export function BreakoutRoomDirectory({eventId}:{eventId:string}){const rooms=getBreakoutRooms(eventId);return <div className="space-y-6"><div className="rounded-3xl bg-white p-6"><h1 className="text-3xl font-semibold">Breakout rooms</h1></div><SectionCard title="Available rooms"><div className="grid gap-4 md:grid-cols-2">{rooms.map(room=><div key={room.id} className="rounded-2xl border border-slate-200 p-4"><div className="flex justify-between"><p className="font-semibold">{room.title}</p><StatusBadge status={room.status} tone={room.status==="live"?"good":"neutral"}/></div><p className="mt-1 text-sm text-slate-600">{room.description}</p></div>)}</div></SectionCard></div>}
