# Visible Control Inventory

| Route | Control | Persona | API/Action | Expected State Change | Persistence Readback | Refresh Proof | Failure UX | Test |
|---|---|---|---|---|---|---|---|---|
| product route/state | create/edit/publish event | authorized persona | mapped action | documented state transition | REQUIRED where durable | REQUIRED where durable | classified failure UX | gauntlet/audit |
| product route/state | register attendee | authorized persona | mapped action | documented state transition | REQUIRED where durable | REQUIRED where durable | classified failure UX | gauntlet/audit |
| product route/state | assign crew | authorized persona | mapped action | documented state transition | REQUIRED where durable | REQUIRED where durable | classified failure UX | gauntlet/audit |
| product route/state | approve/reject | authorized persona | mapped action | documented state transition | REQUIRED where durable | REQUIRED where durable | classified failure UX | gauntlet/audit |
| product route/state | archive/restore | authorized persona | mapped action | documented state transition | REQUIRED where durable | REQUIRED where durable | classified failure UX | gauntlet/audit |
| product route/state | revoke access | authorized persona | mapped action | documented state transition | REQUIRED where durable | REQUIRED where durable | classified failure UX | gauntlet/audit |
| product route/state | send communication | authorized persona | mapped action | documented state transition | REQUIRED where durable | REQUIRED where durable | classified failure UX | gauntlet/audit |
| product route/state | update task/run of show | authorized persona | mapped action | documented state transition | REQUIRED where durable | REQUIRED where durable | classified failure UX | gauntlet/audit |
| product route/state | configure/retry provider | authorized persona | mapped action | documented state transition | REQUIRED where durable | REQUIRED where durable | classified failure UX | gauntlet/audit |
| product route/state | export report | authorized persona | mapped action | documented state transition | REQUIRED where durable | REQUIRED where durable | classified failure UX | gauntlet/audit |
| `/app/events/new` | New event (NOW → Create & open; LATER → Create) | owner cookie / operator cookie / staff session | `createEventAction` → `createEventRecord` (runtime store) | one `runtime_events` row: `live` (NOW) or `draft` (LATER), join code + 5 access codes minted | REQUIRED — `/app/events`, `/app/events/[id]`, `/join?code=` | REQUIRED — row survives reload; overlay rehydrates per request | named stop when tables missing; inline error otherwise | `owner-real-events-journey.spec.ts` (a)(b), `eventRepository.test.ts` |
| `/app/events/[id]`, `/app/events/[id]/publish` | Publish / Go live / End / Back to draft | owner cookie / operator cookie / staff session | `publishEventAction` → `setEventStatus` | `status` on the row; `/join` resolution changes accordingly | REQUIRED — `/join?code=`, `/events/[slug]` | REQUIRED | inline error | `owner-real-events-journey.spec.ts` (b) |
| `/app/events/[id]` | Archive / Restore | owner cookie / operator cookie / staff session | `archiveEventAction` / `restoreEventAction` | `status: archived` + `statusBeforeArchive`; hidden from lists and `/join` until restored | REQUIRED — `/app/events?showArchived=1` | REQUIRED | inline error | `owner-real-events-journey.spec.ts` (c) |
| `/app/events/[id]/access` | Copy crew / speaker / sponsor / VIP / client code | owner cookie / operator cookie / staff session | read of the row's minted codes; gates: `/production-access/crew`, `/production-access/special-guest` | none (read) | n/a | n/a | seed events show env-key names only | `day1-showtime-master-gauntlet.spec.ts`, `cross-event-scope-isolation.spec.ts` |
| `/app/clients` | Create client | owner cookie / staff session | `createClientAction` → `createClientRecord` | one `runtime_clients` row | REQUIRED — `/app/clients`, `/app/clients/[id]` | REQUIRED | inline error | `owner-real-events-journey.spec.ts` (e) |
| `/app/settings` | Save settings (agency name, colours, members) | owner cookie / staff session | `saveAgencySettingsAction` → `saveAgencySettings` | one `runtime_agency_settings` row | REQUIRED — `/app` header | REQUIRED | inline error; operator cookie disabled | `owner-real-events-journey.spec.ts` (e) |
| `/production-access` | Owner Access card → `/production-access/owner` | public visitor | navigate | none | n/a | n/a | n/a | `production-access.spec.ts`, `operator-launchpad-common-sense.spec.ts` |
| `/app/events/[id]`, `/admin/testing/[id]` (chat moderation queue) | Hide / Restore a chat message | owner cookie / operator cookie / event-scoped crew cookie | `moderateLiveChatMessage` → `setLiveChatMessageVisibility` (runtime store `live_chat_messages.moderation_status`, `moderated_by`, `moderated_at`) | message hidden from every attendee listing; crew see it tagged "Hidden by <role>"; restore reverses | REQUIRED — `/venue/[id]/stage` attendee view, queue row tag | REQUIRED — status survives reload | attendee/anonymous: action throws, nothing written | `crew-chat-moderation.spec.ts`, `liveChatModeration.test.ts` |
| `/app/events/[id]`, `/admin/testing/[id]` (chat moderation queue) | Silence / Unsilence an attendee in a room | owner cookie / operator cookie / event-scoped crew cookie | `silenceLiveChatAttendee` → `setLiveChatAttendeeSilence` (`live_chat_moderation_states`, scope `attendee`) | attendee's chat input replaced by "You have been silenced by the crew"; posts rejected on the write path | REQUIRED — `/venue/[id]/stage` for that attendee, "Silenced attendees" list | REQUIRED | attendee/anonymous: action throws | `crew-chat-moderation.spec.ts`, `liveChatModeration.test.ts` |
| `/app/events/[id]`, `/admin/testing/[id]` (chat moderation queue) | Lock / Unlock room chat | owner cookie / operator cookie / event-scoped crew cookie | `lockLiveChatRoom` → `setLiveChatRoomLock` (`live_chat_moderation_states`, scope `room`) | every attendee in the room sees "Chat is locked by the crew"; posts rejected on the write path; crew may still post | REQUIRED — `/venue/[id]/stage`, room lock card | REQUIRED | attendee/anonymous: action throws | `crew-chat-moderation.spec.ts`, `liveChatModeration.test.ts` |
| `/crew/events/[id]`, `/app/events/[id]`, `/admin/testing/[id]` (attendee roster) | Permit to watch / Approve to publish / Revoke / Decline / Reset per attendee row; Approve / Decline per pending request | owner cookie / operator cookie / event-scoped crew cookie | `decideAttendeeLiveAccess` → `applyAttendeeLiveDecision` (runtime store `attendee_live_capabilities`) | capability flags + `requestStatus`; revoke also removes the LiveKit participant | REQUIRED — `/venue/[id]/stage` shows pending / approved / revoked / permitted; roster status pill | REQUIRED — capability survives reload | attendee/anonymous: action throws, nothing written | `crew-attendee-roster.spec.ts`, `attendeeLiveRoster.test.ts` |
| `/venue/[id]/stage` | Request to Join Stage | registered attendee | `requestAttendeeStageAccess` → `recordAttendeeStageRequest` | `requestStatus: requested` (grants nothing) → appears in Pending requests on every crew surface | REQUIRED — crew roster pending list | REQUIRED | unregistered: no form | `crew-attendee-roster.spec.ts` |
| `/crew/events/[id]`, `/app/events/[id]`, `/admin/testing/[id]` | Save main stage / breakout controls (camera, mic, approval, kill switch) | owner cookie / operator cookie / event-scoped crew cookie | `updateAttendeeLiveControl` → `setAttendeeLiveControlState` | room control state | REQUIRED — attendee stage request form appears/disappears | REQUIRED | attendee/anonymous: action throws | `crew-attendee-roster.spec.ts` |
| `/crew/events/[id]`, `/app/events/[id]`, `/app/events/[id]/publish` (while live), `/admin/testing/[id]` | End the show | owner cookie / operator cookie / event-scoped crew cookie | `endTheShow` → `endShowForEvent` (stage signal `operator_mark_show_ended` + `setEventStatus(ended)`) | stage `ENDED`, `operatorMarkedShowEnded`, event `status: ended`; a later `ingress_ended` (webhook or poll) stays `ENDED` instead of failing over to Daily | REQUIRED — `/api/video/stage-stream-state`, control shows ENDED badge | REQUIRED | attendee/anonymous: action throws; seed events: stage mark only | `end-the-show.spec.ts`, `endShowOrdering.test.ts` |
| `/admin/testing/[id]` | LiveKit webhook registration help (URL + copy) and "Last webhook" path card | owner cookie / operator cookie | read of request host + stage state (`lastWebhookEvent`, `lastHealthCheckAt`) | none (read) | n/a | n/a | "None yet" when nothing has polled | `end-the-show.spec.ts` |
| `/speaker/events/[id]`, `/sponsor/events/[id]`, `/venue/[id]/lobby` (VIP) | Tell us who you are (name / company / title, once) | speaker / sponsor / VIP / client code holder | `registerGuestIdentityAction` → `registerGuestIdentity` (`special_guest_profiles` + `wpl_guest_identity` cookie) | one profile row per person per event, bound to the browser; the crew roster lists speakers by name | REQUIRED — portal shell shows the name; crew speaker roster | REQUIRED | attendee/anonymous: role check throws; empty name: inline error | `speaker-green-room.spec.ts`, `speakerGreenRoom.test.ts` |
| `/speaker/events/[id]/tech-check` | Record my tech check | identified speaker | `recordSpeakerTechCheckAction` → `recordSpeakerTechCheck` (`event_guest_states` kind `speaker_tech_check`) | readiness + score + checks on the speaker's roster row | REQUIRED — crew speaker roster row, green room summary | REQUIRED | attendee: role check throws | `speaker-green-room.spec.ts` |
| `/crew/events/[id]`, `/app/events/[id]`, `/admin/testing/[id]` (speaker roster) | Bring to stage / Send backstage | owner cookie / operator cookie / event-scoped crew cookie | `bringSpeakerToStageAction` / `sendSpeakerBackstageAction` (`speaker_stage`); send backstage also `removeLiveKitParticipantFromMainStage` | speaker's main-stage publish grant on / off; green room shows "Go on stage" | REQUIRED — speaker green room + on-stage page; `/api/video/livekit-token` refuses the stage until invited | REQUIRED | attendee/speaker/anonymous: action throws | `speaker-green-room.spec.ts`, `speakerGreenRoom.test.ts` |
| `/speaker/events/[id]/green-room` | Go on stage | invited speaker | `goOnStageAction` → `markSpeakerOnStage` | `on_stage`; `/speaker/events/[id]/backstage` renders the main-stage LiveKit room with the publish grant | REQUIRED — crew roster row shows on stage | REQUIRED | backstage speaker: no self-grant, returns to green room with a notice | `speaker-green-room.spec.ts` |
| `/crew/events/[id]`, `/app/events/[id]`, `/app/events/[id]/approval-queue` | Cue cards: Save as live version / Approve / Discard / Push cue; Producer notes to speakers | owner cookie / operator cookie / event-scoped crew cookie | `saveProducerCueDeckAction`, `approveSpeakerCueDeckAction`, `pushLiveCueAction`, `saveProducerNotesAction` (`speaker_cue_deck`, `speaker_live_cue`, `producer_notes`) | approved / pending deck versions per speaker; live cue; notes | REQUIRED — speaker teleprompter polls `/api/speaker/cue-deck` every ~5s and shows the "producer pushed a change" and live-cue banners | REQUIRED | attendee/speaker/anonymous: action throws | `speaker-green-room.spec.ts`, `speakerGreenRoom.test.ts` |
| `/speaker/events/[id]/teleprompter` | Queue for producer review (paste cue cards / talking points / script / notes) | speaker code holder | `submitSpeakerCueDeckAction` → `saveCueDeckVersion` (pending version) | pending version; approved deck unchanged until the crew approves | REQUIRED — crew roster pending block, approval-queue pending list | REQUIRED | attendee: role check throws | `speaker-green-room.spec.ts`, `day1-showtime-master-gauntlet.spec.ts` |
| `/sponsor/events/[id]`, `/sponsor/events/[id]/booth` | Save booth (name, blurb, link) | identified sponsor | `saveSponsorBoothAction` → `saveSponsorBooth` (`sponsor_booth`) | booth published to `/venue/[id]/expo` once named | REQUIRED — Expo lists the booth; booth page shows the link | REQUIRED | attendee: role check throws | `speaker-green-room.spec.ts` |
| `/crew/events/[id]`, `/app/events/[id]` | Open / Close the VIP lounge | owner cookie / operator cookie / event-scoped crew cookie | `setVipRoomAction` → `setVipRoomOpen` (`vip_room`) | VIP code holders see the lounge on the lobby | REQUIRED — `/venue/[id]/lobby` for a VIP cookie | REQUIRED | attendee/anonymous: action throws; visitors never see the panel | `speaker-green-room.spec.ts` |
| `/production-access/owner` | Enter owner workspace (either owner master password) | owner | `enterOwner` / `POST /api/production-access/owner` → `matchOwnerMasterPassword` | signed owner cookie carrying `ownerKey` (`primary` \| `secondary`); lands on `/app` | audit: `access_granted owner_master:<key>` | cookie survives reload; gates skip re-prompt | invalid → `?error=invalid`, audit `access_denied` | `production-access.spec.ts`, `ownerMasterPassword.test.ts` |

Navigation is not a durable mutation. Source presence is not proof.
